IL a fallut créer un environnement avec les commandes : 
	pip install virtualenv
	virtualenv my_sql_env  #Choix du nom arbitraire

De plus, il faut:
	pip install --upgrade google-cloud-storage



############_Command line to connect to the mySQL from goole Cloud Shell_############
CARE : this is the 
gcloud sql connect <my-sql-project> --user=root

